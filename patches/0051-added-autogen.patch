From 7c91a9975bd631f71be67030ac48979fc2c03e7a Mon Sep 17 00:00:00 2001
From: Yegor Dia <TheDialord@users.noreply.github.com>
Date: Thu, 11 Aug 2016 05:59:08 +0100
Subject: [PATCH 51/63] added autogen

---
 varnish4-vmods/libvmod-var-4.0/autogen.sh | 44 +++++++++++++++++++++++++++++++
 1 file changed, 44 insertions(+)
 create mode 100755 varnish4-vmods/libvmod-var-4.0/autogen.sh

diff --git a/varnish4-vmods/libvmod-var-4.0/autogen.sh b/varnish4-vmods/libvmod-var-4.0/autogen.sh
new file mode 100755
index 0000000..2e1fb89
--- /dev/null
+++ b/varnish4-vmods/libvmod-var-4.0/autogen.sh
@@ -0,0 +1,44 @@
+#!/bin/sh
+
+warn() {
+	echo "WARNING: $@" 1>&2
+}
+
+case `uname -s` in
+Darwin)
+	LIBTOOLIZE=glibtoolize
+	;;
+FreeBSD)
+	LIBTOOLIZE=libtoolize
+	;;
+Linux)
+	LIBTOOLIZE=libtoolize
+	;;
+SunOS)
+	LIBTOOLIZE=libtoolize
+	;;
+*)
+	warn "unrecognized platform:" `uname -s`
+	LIBTOOLIZE=libtoolize
+esac
+
+automake_version=`automake --version | tr ' ' '\n' | egrep '^[0-9]\.[0-9a-z.-]+'`
+if [ -z "$automake_version" ] ; then
+	warn "unable to determine automake version"
+else
+	case $automake_version in
+		0.*|1.[0-8]|1.[0-8][.-]*)
+			warn "automake ($automake_version) detected; 1.9 or newer recommended"
+			;;
+		*)
+			;;
+	esac
+fi
+
+set -ex
+
+$LIBTOOLIZE --copy --force
+aclocal -I m4
+autoheader
+automake --add-missing --copy --foreign
+autoconf
-- 
2.7.4

